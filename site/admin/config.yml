backend:
  name: git-gateway
  branch: master

publish_mode: editorial_workflow

media_folder: "images"

collections:
  - label: "Pages"
    name: "pages"
    files:
      - label: "Homepage"
        name: "homepage"
        file: "index.json"
        fields:
          - {label: Header, name: header, widget: string}
